export const signup = async (req, res) => {
  res.json({ data: "You hit up the sing in endpoint" });
};

export const login = async (req, res) => {
  res.json({ data: "You hit up the login in endpoint" });
};

export const logout = async (req, res) => {
  res.json({ data: "You hit up the logout in endpoint" });
};
